# Decision lineage — project documents

**As of** 2026-08-31.

The dated change lineage for the project-level documents, one section per document, oldest first
within each. Entries were moved here from the feet of `PROCESS.md` and `SPEC.md` on 2026-08-31
(see that date's entry below); each source document keeps a one-line pointer to this file. New
project-level decisions append here, under the document they amend. Task-level decision lineages
live in each task's own `docs/tasks/<task-slug>/decisions.md`.

## docs/PROCESS.md

- **2026-08-27** (`rung0-replicate-ceiling`) — four changes, motivated by the first task run under this manual:
  §1 gained the brainstorming approval gate — the first rung-0 session began editing documents on a revision request that had not approved the design; the gate existed in the brainstorming skill but not here, and this manual is what sessions follow.
  §3's known-answer bullet was rewritten in positive/negative-control vocabulary bound to SPEC project rule 4, and a bullet was added requiring the minimum detectable effect beside every promoted comparison — one requirement, one vocabulary, across both documents.
  §4's "one branch at a time" (which governed only branch timing, with naming in a separate bullet) was tightened to one-task-one-branch cut from the trunk, and a bullet was added requiring the branch to carry its result's full provenance chain of scripts.
  §2's standing commit/push grant (previously unscoped `origin`) was scoped to the current task's branch, effective on design approval.
- **2026-08-27** (`rung0-replicate-ceiling`, later the same day) — §5 gained the landed-references rule during rung 0's spec review: the new dataset registry and design documents had cited datasets and derivations from the unlanded prior lineage, which a reviewer of the public repository cannot open. References in the sense of `docs/SPEC.md` remain correct; references into worktrees do not.
- **2026-08-27** (`rung0-replicate-ceiling`, later still) — the landed-references bullet gained the path form: cluster files cited repository-relative plus "on Alpine", never by absolute site paths. The first design document to cite a cluster file had written the absolute path, username and mount layout included.
- **2026-08-28** (`rung0-replicate-ceiling`) — two lifecycle stages added between Promote and Close out, at Lucas's direction after rung 0 reached its pull request: **Audit** (the design read as numbered claims against the landed tree, every departure aligned or recorded — the systematic drift check the per-task reviews and dated entries approximate but do not guarantee) and **Summarise** (the plain-language `summary.md` a lab reviewer reads first). §6's definition of done gained the matching bullet; the PR opens only after both.
- **2026-08-28** (`rung0-replicate-ceiling`, after its first audit) — the Audit stage moved from after Promote to **before** it, with an explicit fix wave: rung 0's audit ran post-promotion and found a declared-vs-scored panel discrepancy the promotion should have waited for. Close out now opens the pull request as a draft, with a final human review of the task documents before it is marked ready. §3 gained two principles from the same discussion: a cheap verification check beats an argued assumption, and an assumption is stated with its quantitative exposure, never as a bare hedge.
- **2026-08-28** (`rung0-replicate-ceiling`, second entry) — the audit's artifact moved to its own document, `docs/tasks/<task-slug>/audit.md` (was: a section in `review.md`), carrying the full clause table, the fix wave's dispositions, and a re-audit by a fresh reader — the audit does not pass until the re-audit confirms every drift item fixed or recorded. The reason is structural, at Lucas's direction: agentic development drifts silently unless independent re-verification is built in as a standing redundancy; find-then-independently-confirm is the same two-stage pattern the per-task code reviews already use, now applied to the documents themselves.
- **2026-08-28** (`rung0-replicate-ceiling`, third entry) — §1 gained the convergence paragraph: the lifecycle runs per task, late additions ripple downstream incrementally (scoped review, amended verification, audit delta with fresh-reader confirmation, amended summary), and promotion re-triggers only when a promoted claim changes, via §6's correction path. Prompted by rung 0's derangement check, added after the audit had passed: the alternative readings — restart the cycle per run, or let late work skip the gates — are both failure modes.
- **2026-08-28** (`rung0-replicate-ceiling`, fourth entry) — §5 gained the acronym rule at Lucas's direction: every acronym spelled out at first use in every report document. Prompted by rung 0's summary using MDE undefined; the same review asked for the summary's assumption caveat in plain language, which is the standing register for `summary.md`.
- **2026-08-31** (`rung0-replicate-ceiling`, lab-meeting review of pull request 14) — the dated change lineages moved out of the documents they describe, at Lucas's direction: project-level entries (this manual's and `docs/SPEC.md`'s Changes sections) to this file, task-level entries (the dated feet of each task's `design.md` and `plan.md`) to that task's `docs/tasks/<task-slug>/decisions.md`, with a one-line pointer left at each source. The lineage grows without bound at the foot of documents readers open for current guidance; splitting history from current position keeps both readable. SPEC rule 2's location wording and its edge-case test moved with the content.
- **2026-08-31** (`rung0-replicate-ceiling`, same review) — §1's Implementation plan row now requires plans to state interfaces, invariants, ordered steps, and expected test outcomes — never embedded full source. Rung 0's plan carried every function verbatim, and five of the drift audit's findings were nothing but plan-copy vs shipped-copy divergences; code that exists twice drifts, so it exists once, in the repository.
- **2026-08-31** (`rung0-replicate-ceiling`, same review) — §1's convergence paragraph gained the cap: documentary findings batch into a single fix wave, the re-audit re-checks only the items verdicted drift, and there is no confirmation pass of a confirmation pass; mechanical claims (counts, tallies, the existence of a cross-reference) are checked by script, never by a fresh reader. Rung 0's audit deltas kept finding documentary defects introduced by the previous fix wave, and one pass was spent reconciling the audit's own clause counting — credits spent on arithmetic a script does for free.
- **2026-08-31** (`rung0-replicate-ceiling`, same review) — §3 gained the executable-verification bullet: every task ships a verify entry point — a script plus a notebook committed without outputs, so the reviewer executes it and watches the checks pass — that recomputes each promoted claim from the committed artifacts alone, with the summary's evidence table checked against the artifacts mechanically and a continuous integration test running the same checks. Prompted by the lab meeting's question "how can we trust the outputs in the summary?": trust comes from re-derivation the reader performs, not from narrative the writer asserts, and a number recomputed at read time cannot drift the way a transcribed one can. Corrected the same day, at Lucas's direction, after the first notebook shipped as a thin wrapper calling the script ("no one is going to trust that"): wrapper cells relocate the trust into the script instead of discharging it, so the notebook's cells must recompute inline and self-contained — standard-library hashing, direct table reads, explicit arithmetic, nothing imported from the project's own code — with the script as the final cross-check cell and the continuous-integration form.
- **2026-08-31** (`rung0-replicate-ceiling`, same review) — Close out now requires the pull request description to separate the review surface (the files needing human eyes) from generated evidence (bulk artifacts committed so a reviewer can re-derive results without the cluster), with generated files marked `linguist-generated` in `.gitattributes` so they collapse in the diff view. Rung 0's pull request added roughly 26,000 lines of which roughly 17,500 were generated comma-separated values (CSV) files, and the lab review read that as review burden rather than as evidence.

## docs/SPEC.md

- **2026-08-27** (`rung0-replicate-ceiling`) — Rung 0's measure now declares the ladder-wide statistic (per-pair Pearson, mean over pairs) with stratified nulls and a positive control, and rung 1's measure references the same statistic. Previously the aggregation was undeclared, and the old lineage had already paid for that: its rung-0 headline was a median while its rung 1 reported means, blocking fraction-of-ceiling. Project rule 4 added — positive and negative controls per measurement step, minimum detectable effect beside every promoted comparison. First task indexed in the spec tree: `rung0-replicate-ceiling`.
- **2026-08-27** (`rung0-replicate-ceiling`, spec review) — The evaluation **frame** added to the vocabulary, out of a discussion of rung 0's drug scope: should the ceiling be computed over all Tahoe drugs, or over the intersection the upper rungs can score? The intersection — a ceiling must be measured on the pairs it will denominate, and an all-corpus ceiling would shift with drug-composition effects unrelated to the assay. Generalized: tranches, panels, and statistic are frame elements; a new tranche or panel opens a new frame and recomputes the rungs within it, while prior frames' promoted results stand under their own provenance. Rung 0's measure now names the declared drug panel beside the gene panel.
- **2026-08-28** (`rung0-replicate-ceiling`) — The Process summary gained the two close-out stages added to PROCESS §1 at Lucas's direction: a clause-level drift audit of the design against the landed tree, and a plain-language summary, both before the pull request.
- **2026-08-28** (`rung0-replicate-ceiling`, after its first audit) — the audit moved before promotion in the Process summary, matching PROCESS §1's reorder: rung 0's own audit, run post-promotion, found a declared-vs-scored panel discrepancy that promotion should have waited for. Pull requests now open as drafts for one final review round.
- **2026-08-31** (`rung0-replicate-ceiling`, lab-meeting review of pull request 14) — rule 2's location amended: dated reversal entries live in each task's `decisions.md` (project-level entries in `docs/decisions.md`), not at the feet of `design.md` and `plan.md`; the edge-case test accepts a new dated entry in either the rewritten document or the task's `decisions.md`. Part of the lineage relocation recorded under PROCESS.md at this date.
